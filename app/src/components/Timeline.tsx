/** Bottom timeline. The year slider and event ticks arrive with the atlas in Phase 2. */
export function Timeline() {
  return (
    <footer className="timeline" data-testid="timeline" aria-label="Timeline">
      <p className="placeholder">Timeline</p>
    </footer>
  );
}
