# 2026-09-17 — Phase 3 closes: the determinism gate and Sitting B

Standing rule from today: every PR merges when CI is green unless the brief says otherwise.

## A. PR #9 (Sitting A) merged

Merged as `97cdc4b`.

## B. PRNG reference and the cross-engine determinism gate

- The standard mulberry32 and its pinned fixture are the reference; the township-generator question is
  closed that way (`docs/decisions.md`).
- **`npm run determinism -w app`** launches Chromium, WebKit and Firefox, runs two fixed cases on the
  committed mesh (Alberta, bisect, N=15; Canada, equal population, N=10; seed 20260917) and asserts
  identical assignment hashes in all three, equal to the hashes Vitest gets in Node
  (`app/src/engine/golden/cross-engine.json`). First run: all four agree. It runs in CI after the smoke
  tests, and replaces the two-device check in `docs/plan.md`.

## C. Sitting B — UI, constraints and presets

Branch `phase-3b`. Decisions are under **2026-09-17 — Phase 3 Sitting B** in `docs/decisions.md`.

### Built, by brief item
1. **Generate panel** (a tab in the right panel): scope (Canada, province, atlas unit at the current
   date, a region of the current split, a drawn polygon), method, N, seed, lens presets (Economic,
   Demographic, Linguistic, Indigenous-constitutional, Physical, Political, Internal-colony index)
   with per-column sliders and "add a column", balance target, contiguity, compactness weight, run and
   cancel with progress from the worker.
2. **Constraints:** min and max population per region; keep-together and keep-apart pins, picked by
   clicking the map or searching the gazetteer; carve CMAs above a threshold first; snap layers
   (rivers, basins, Continental Divide, treaties, CD and CSD edges, 0.5° graticule, ridings, ecozones)
   with a snap weight. Township lines are listed as unavailable: no DLS data yet.
3. **Rendering:** regions dissolved from the hex topology in the app, eight hues assigned so
   neighbours differ, region names on the map, hover stats, legend with per-region details; the map
   fits a new split's scope.
4. **Manual override:** select a region, paint cells; stats and names recompute; `edited` and the edit
   log go into the downloaded pack.
5. **URL state:** `#split=` reruns a spec, `#pack=` loads a preset; followed on load and on hashchange.
6. **Presets** (amended; no hand-drawn templates): alberta-15, canada-26 and canada-14, generated by
   the engine into `app/public/packs/` with their full spec; `npm run presets -w app` regenerates them.

### New pipeline artefacts
`places.v1.json.gz` (124 KB), `cells.v1.topojson.gz` (1.44 MB), `snap.v1.json.gz` (48 KB), built by
the layers step. `data/build` is 7.6 MB.

### Problems found on the way, and what was done
- **The capitals preset was wrong twice.** Balanced growth let the St. John's region reach Laval; then,
  with no balance target, annealing optimised compactness alone and Edmonton's region lost Edmonton.
  The preset is now growth only: the land nearest each capital.
- **Carving Toronto missed 1.2 million people:** cell centres in Lake Ontario. CMA membership now
  goes through each cell's CSD.
- **Region names:** naming by the most populous place's point misnamed regions holding most of a split
  city ("Clearwater County", 616k people). Names now come from the CSD with the most people inside the
  region, and are unique.
- **A share link pasted into an open tab did nothing:** only the initial hash was read. The app now
  listens for hashchange.

### Memory on the Codespace
- The VS Code Playwright extension's `test-server` was running alongside `make verify`; it was stopped
  (available memory 2,994 → 3,478 MB) and kept off by a watcher until verify exited. The watcher's
  first version matched its own command line and stopped itself; the second, a script file, did not.
- Smoke now runs with `workers: 1`; the determinism test launches one engine at a time. The serial
  order is in `docs/perf.md`. Nothing was killed.

### Gate
| Step | Result | Lowest available memory |
|---|---|---|
| `make verify` | **clean**, every artefact byte for byte (run alone) | — |
| pytest | 169 passed | 3,110 MB |
| `validate.py` | 19/19 | 3,811 MB |
| `npm run build` | clean | 3,587 MB |
| Playwright smoke (desktop, iPad) | 22/22 | 3,036 MB |
| Determinism (Chromium, WebKit, Firefox) | identical hashes, equal to Node | 3,165 MB |
| Vitest | 97 passed | — |

- **Found by smoke:** on the iPad layout the bottom sheet covers the map, so the paint test's click on
  Toronto landed on the legend. The test now closes the sheet before painting. The app was right.
- Amended Phase 3 gate: goldens committed ✓; every region contiguous under hard mode ✓; cross-engine
  determinism ✓; N=1 to 30 on Canada, no crash and no split region ✓.

## Merged and tagged

PR #10 merged as `8c68abe`. Phase 3 is complete. Tagged **`pre-phase4`** and **`v0.3-splitter`** on
main. Phase 4 (dossiers and set analysis) is not started.

## D. Phase 4 prep (PR #11)

- **Seeded seeds are pinned:** refinement may never move the cell a region grew from, so a capital
  always stays in its own region. canada-14 is regenerated with refinement on, and a test checks all 14.
- **Found on the way:** `balance: null` still ran a balance term, because the load fell back to 1 per
  cell — "no balance target" silently meant "equal cell counts". canada-14 had Edmonton's region at
  0.61M against Regina's 4.04M; now 4.69M and 1.05M.
- The internal-colony lens shows its formula in the panel; `docs/backlog.md` records the DLS township
  sources (AltaLIS and the SK and MB equivalents) for the disabled township snap layer.

## E. Phase 4 — dossiers and set analysis

Branch `phase-4`. Decisions are under **2026-09-17 — Phase 4: dossiers and set analysis**.

### Built
- `app/src/dossier/`: per-region aggregates, the name generator, borders in words, the four written
  fields, the federalism rules and their evaluators, the set analysis, and the Markdown export.
- Pipeline: the snap artefact now carries the name of the river each edge crosses (549 named rivers).
- App: three new panel tabs — **Region** (the dossier), **Set** (power ranking, ratios, reconciliation,
  contiguity compromises, federalism panel) and **Compare** (another preset or actual Canada, with a
  swipe divider on the map and a difference table). Markdown download from either panel.
- Packs carry their dossiers and set analysis; the three presets are regenerated through the same code
  path the test uses.

### What the presets now say
- alberta-15's regions are named Athabasca, Oldman, Peace, Smoky, Wabasca, Boreal Plains…
- canada-26 breaks Senate divisions, Quebec's asymmetry and territorial status; alberta-15 holds them
  all; provinces-as-regions holds the three structural rules.

### Phase 4 gate
| Step | Result | Lowest available memory |
|---|---|---|
| `make verify` (snap carries river names) | **clean**, byte for byte (run alone) | — |
| pytest | 169 passed | 3,333 MB |
| `validate.py` | 19/19 | 3,745 MB |
| Vitest | 113 passed | 3,380 MB |
| Playwright smoke (desktop, iPad) | 28/28 | 2,435 MB |
| Determinism (Chromium, WebKit, Firefox) | identical hashes | 3,114 MB |

- Compare mode passes on the iPad layout (820×1180), as the brief requires.
- **Found by smoke:** the first dossier test waited for the Generate panel's legend after switching
  tabs, which unmounts it. The test now uses the Region tab's own list.
